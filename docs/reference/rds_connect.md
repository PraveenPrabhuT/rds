## rds connect

Connect to an RDS PostgreSQL instance

### Synopsis

Connect dynamically fetches credentials from AWS Secrets Manager 
and establishes a connection using pgcli, psql, or a native Go fallback.
It requires an active Pritunl VPN connection matching the AWS Profile.

Supports connecting by instance name, RDS host endpoint, or JDBC URL.
Credentials are always resolved from Secrets Manager automatically.

```
rds connect [rds-identifier] [flags]
```

### Examples

```
  # Interactive selection
  rds connect

  # Direct connection with partial name
  rds connect acko-health

  # Reconnect to the last used instance for the current profile
  rds connect -l

  # Connect to a specific database
  rds connect my-instance --db myapp

  # Connect by RDS host endpoint
  rds connect --host my-rds.abc.ap-south-1.rds.amazonaws.com

  # Connect via JDBC URL (DNS alias resolved automatically)
  rds connect --url 'jdbc:postgresql://my-db.internal.example.com/myapp'

  # Get JDBC URL for app config and copy to clipboard
  rds connect my-instance --jdbc --copy
```

### Options

```
      --copy          Copy JDBC URL to clipboard (use with --jdbc)
  -d, --db string     Database name to connect to (default "postgres")
  -h, --help          help for connect
      --host string   RDS host endpoint (bypasses instance picker)
      --jdbc          Print JDBC URL after resolving credentials
  -l, --last          Connect to the last used RDS instance
      --port int      PostgreSQL port (default 5432)
      --url string    JDBC URL to connect (jdbc:postgresql://host[:port][/database])
```

### Options inherited from parent commands

```
  -p, --profile string   AWS profile to use (default "ackodev")
  -r, --region string    AWS Region (overrides config/env)
```

### SEE ALSO

* [rds](rds.md)	 - A powerful CLI toolkit for AWS RDS management

###### Auto generated by spf13/cobra on 9-Mar-2026
