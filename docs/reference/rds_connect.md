## rds connect

Connect to an RDS PostgreSQL instance

### Synopsis

Connect dynamically fetches credentials from AWS Secrets Manager 
and establishes a connection using pgcli, psql, or a native Go fallback. 
It requires an active Pritunl VPN connection matching the AWS Profile.

```
rds connect [rds-identifier] [flags]
```

### Examples

```
  # Interactive selection
  rds connect

  # Direct connection with partial name
  rds connect acko-health

  # Reconnect to the last used instance for the current profile
  rds connect -l
```

### Options

```
  -h, --help   help for connect
  -l, --last   Connect to the last used RDS instance
```

### Options inherited from parent commands

```
  -p, --profile string   AWS profile to use (default "ackodev")
```

### SEE ALSO

* [rds](rds.md)	 - A powerful CLI toolkit for AWS RDS management

###### Auto generated by spf13/cobra on 5-Jan-2026
