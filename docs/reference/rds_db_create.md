## rds db create

Create a database with users and permissions on an RDS instance

### Synopsis

Create provisions a new PostgreSQL database on an RDS instance along with
a standard set of users and permissions matching the organization's playbook:

  - Migration user (all privileges on the database)
  - Read-only users (v1 and v2 with role inheritance)
  - Read-write users (v1 and v2 with role inheritance)
  - IAM database user for AWS IAM authentication

Superuser credentials are fetched automatically from AWS Secrets Manager.
Read replicas are filtered out from the instance picker.

```
rds db create [db-name] [flags]
```

### Examples

```
  # Interactive instance and database name selection
  rds db create

  # Create database "Pricing" with interactive instance picker
  rds db create Pricing

  # Target a specific RDS host
  rds db create Pricing --host my-rds.abc.ap-south-1.rds.amazonaws.com

  # Dry run to preview SQL without executing
  rds db create Pricing --dry-run

  # Use custom schema and connection limits
  rds db create Pricing --schema app --migration-conn-limit 20
```

### Options

```
      --default-db string          Initial database for user creation (default "postgres")
      --dry-run                    Print SQL statements without executing
  -f, --force                      Skip existing database/users instead of failing
  -h, --help                       help for create
      --host string                RDS host endpoint (bypasses instance picker)
      --migration-conn-limit int   Connection limit for migration user (default 10)
      --port int                   PostgreSQL port (default 5432)
      --ro-conn-limit int          Connection limit for read-only users (default 10)
      --rw-conn-limit int          Connection limit for read-write users (default 10)
  -s, --schema string              Target schema for privilege grants (default "public")
```

### Options inherited from parent commands

```
  -p, --profile string   AWS profile to use (default "ackodev")
  -r, --region string    AWS Region (overrides config/env)
```

### SEE ALSO

* [rds db](rds_db.md)	 - Database management commands

###### Auto generated by spf13/cobra on 9-Mar-2026
